services:
  - type: web
    name: baccarat-backend
    env: python
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "bash backend/scripts/render_start.sh"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: baccarat-db
          property: connectionString
      - key: JWT_SECRET_KEY
        sync: false
      - key: DEBUG
        value: "false"
      - key: ENVIRONMENT
        value: "production"
      - key: CORS_ORIGINS
        sync: false
      - key: ADMIN_DEFAULT_PASSWORD
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: SINGLE_AI_API_KEY
        sync: false
      - key: SINGLE_AI_MODEL
        sync: false
      - key: SINGLE_AI_API_BASE
        sync: false

  - type: web
    name: baccarat-frontend
    env: static
    buildCommand: "cd frontend && npm install && npm run build"
    staticPublishPath: "./frontend/dist"
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: baccarat-backend
          envVarKey: RENDER_EXTERNAL_URL
      - key: VITE_WS_URL
        fromService:
          type: web
          name: baccarat-backend
          envVarKey: RENDER_EXTERNAL_URL

databases:
  - name: baccarat-db
    plan: free
    postgresMajorVersion: 15
