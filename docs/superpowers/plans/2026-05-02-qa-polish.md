# QA 收尾优化（文案一致性 + /ws HTTP 明确拒绝）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 3 个 QA 发现的小问题：登录按钮文案、修改密码弹窗标题、/ws 普通 HTTP 访问返回 426。

**Architecture:** 前端仅改文案；后端新增一个 HTTP GET `/ws` 作为“非 WebSocket Upgrade 的明确拒绝”，保持现有 WebSocket token 校验与广播逻辑不变。

**Tech Stack:** React + Ant Design + FastAPI。

---

### Task 1: 前端文案一致性修复

**Files:**
- Modify: `/workspace/frontend/src/components/admin/LoginModal.tsx`
- Modify: `/workspace/frontend/src/pages/AdminPage.tsx`

- [ ] **Step 1: 修改登录按钮文案**

将 LoginModal 的按钮文本从：
- `登录进入管理面板`

改为：
- `登录进入系统`

- [ ] **Step 2: 修改改密弹窗标题**

将 AdminPage 的 Modal title 从：
- `修改默认密码`

改为：
- `修改密码`

- [ ] **Step 3: 前端 build 验证**

Run:
```bash
cd /workspace/frontend && npm run build
```
Expected: build success.

---

### Task 2: 后端 /ws 非 WebSocket 请求返回 426

**Files:**
- Modify: `/workspace/backend/app/api/routes/websocket.py`
- Create: `/workspace/backend/tests/test_ws_http_upgrade_required.py`

- [ ] **Step 1: 增加 GET /ws 路由**

实现行为：
- 普通 HTTP GET `/ws` 返回 426
- body 为 JSON：`{"detail":"请使用 WebSocket Upgrade 连接 /ws"}`

- [ ] **Step 2: 新增单测覆盖**

Create `backend/tests/test_ws_http_upgrade_required.py`:

```python
import os
import sys
import unittest

from fastapi.testclient import TestClient

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))
from app.api.main import app


class WsHttpUpgradeRequiredTest(unittest.TestCase):
    def test_ws_http_get_returns_426(self):
        client = TestClient(app)
        r = client.get("/ws")
        self.assertEqual(r.status_code, 426)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3: 后端全量单测**

Run:
```bash
mkdir -p /workspace/data
python -m unittest discover -s backend/tests -p 'test_*.py' -v
```
Expected: PASS.

