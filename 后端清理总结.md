# 后端清理总结

## 删除的文件

| 文件 | 说明 |
|------|------|
| `app/services/workflow_engine.py` | 爬虫工作流引擎（150秒超时、自动重启、ScraperManager依赖） |
| `app/services/scraper_service.py` | 爬虫服务（HttpApiScraper、HtmlScraper、ScraperManager） |
| `app/services/lile333_scraper.py` | Lile333专用浏览器采集器（Playwright） |

## 修改的文件

| 文件 | 修改内容 |
|------|----------|
| `app/models/schemas.py` | `crawl_stability` → `data_integrity`（采集稳定性→数据完整性） |
| `requirements.txt` | 移除 `playwright==1.58.0` 依赖，更新注释 |

## 保留的文件（手动模式必需）

```
app/services/
├── __init__.py
├── ai_learning_service.py      # AI学习服务
├── betting_service.py           # 下注服务
├── manual_game_service.py       # 手动游戏服务（核心）
├── road_engine.py               # 五路走势计算引擎
├── smart_model_selector.py      # 智能选模
├── three_model_service.py       # 三模型AI服务
└── deprecated/                  # 已弃用的旧模型文件
    ├── intelligent_model_service.py
    ├── model_router.py
    ├── prompts_config.py
    └── three_model_service_v2.py
```

## 当前后端架构

```
手动模式核心流程：
上传开奖记录 → AI三模型分析 → 下注 → 输入开奖结果 → 结算 → 下一局分析

API端点：
- POST /api/games/upload      # 上传开奖记录
- POST /api/games/bet         # 下注
- POST /api/games/reveal      # 开奖
- GET  /api/games/current-state  # 获取当前状态
```

## 清理完成 ✅

所有爬虫、工作流、自动采集相关代码已彻底删除。
后端现在纯粹支持手动模式，无遗留自动采集逻辑。
